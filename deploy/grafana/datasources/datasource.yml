# Grafana datasource provisioning. Mounted read-only at
# /etc/grafana/provisioning/datasources -- see deploy/docker-compose.yml.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
